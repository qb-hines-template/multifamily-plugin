/** How long cached Entrata feed responses remain valid. */
export const FEED_CACHE_TTL_MS = 60 * 60 * 1000;

/** Cache key for the full floorplan feed produced by `generate`. */
export const FLOORPLANS_FEED_CACHE_KEY = 'floorplans-feed';
